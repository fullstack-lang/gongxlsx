basePath: /api/github.com/fullstack-lang/gongxlsx/go/v1
consumes:
- application/json
definitions:
  DeletedAt:
    $ref: '#/definitions/NullTime'
  NullInt64:
    description: |-
      NullInt64 implements the Scanner interface so
      it can be used as a scan destination, similar to NullString.
    properties:
      Int64:
        format: int64
        type: integer
      Valid:
        type: boolean
    title: NullInt64 represents an int64 that may be null.
    type: object
    x-go-package: database/sql
  NullString:
    description: |-
      var s NullString
      err := db.QueryRow("SELECT name FROM foo WHERE id=?", id).Scan(&s)
      ...
      if s.Valid {
      use s.String
      } else {
      NULL value
      }
    properties:
      String:
        type: string
      Valid:
        type: boolean
    title: |-
      NullString represents a string that may be null.
      NullString implements the Scanner interface so
      it can be used as a scan destination:
    type: object
    x-go-package: database/sql
  NullTime:
    description: |-
      NullTime implements the Scanner interface so
      it can be used as a scan destination, similar to NullString.
    properties:
      Time:
        format: date-time
        type: string
      Valid:
        type: boolean
    title: NullTime represents a time.Time that may be null.
    type: object
    x-go-package: database/sql
  XLCell:
    properties:
      Name:
        type: string
      X:
        format: int64
        type: integer
        x-go-name: "Y"
    type: object
    x-go-package: github.com/fullstack-lang/gongxlsx/go/models
  XLRow:
    properties:
      Cells:
        items:
          $ref: '#/definitions/XLCell'
        type: array
      Name:
        type: string
      NbCols:
        format: int64
        type: integer
      RowIndex:
        format: int64
        type: integer
    type: object
    x-go-package: github.com/fullstack-lang/gongxlsx/go/models
  XLSheet:
    properties:
      MaxCol:
        format: int64
        type: integer
      MaxRow:
        format: int64
        type: integer
      Name:
        type: string
      NbRows:
        format: int64
        type: integer
      Rows:
        items:
          $ref: '#/definitions/XLRow'
        type: array
      SheetCells:
        items:
          $ref: '#/definitions/XLCell'
        type: array
    type: object
    x-go-package: github.com/fullstack-lang/gongxlsx/go/models
  xlcellAPI:
    description: |-
      for POST, API, one needs the fields of the model as well as the fields
      from associations ("Has One" and "Has Many") that are generated to
      fullfill the ORM requirements for associations
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name:
        type: string
      UpdatedAt:
        format: date-time
        type: string
      X:
        format: int64
        type: integer
        x-go-name: "Y"
      XLRow_CellsDBID:
        $ref: '#/definitions/NullInt64'
      XLRow_CellsDBID_Index:
        $ref: '#/definitions/NullInt64'
      XLSheet_SheetCellsDBID:
        $ref: '#/definitions/NullInt64'
      XLSheet_SheetCellsDBID_Index:
        $ref: '#/definitions/NullInt64'
    title: XLCellAPI is the input in POST API
    type: object
    x-go-name: XLCellAPI
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlcellDB:
    description: |-
      It incorporates the GORM ID, basic fields from the model (because they can be serialized),
      the encoded version of pointers
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name_Data:
        $ref: '#/definitions/NullString'
      UpdatedAt:
        format: date-time
        type: string
      X_Data:
        $ref: '#/definitions/NullInt64'
      XLRow_CellsDBID:
        $ref: '#/definitions/NullInt64'
      XLRow_CellsDBID_Index:
        $ref: '#/definitions/NullInt64'
      XLSheet_SheetCellsDBID:
        $ref: '#/definitions/NullInt64'
      XLSheet_SheetCellsDBID_Index:
        $ref: '#/definitions/NullInt64'
      Y_Data:
        $ref: '#/definitions/NullInt64'
    title: XLCellDB describes a xlcell in the database
    type: object
    x-go-name: XLCellDB
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlfileAPI:
    description: |-
      for POST, API, one needs the fields of the model as well as the fields
      from associations ("Has One" and "Has Many") that are generated to
      fullfill the ORM requirements for associations
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name:
        type: string
      NbSheets:
        format: int64
        type: integer
      Sheets:
        items:
          $ref: '#/definitions/XLSheet'
        type: array
      UpdatedAt:
        format: date-time
        type: string
    title: XLFileAPI is the input in POST API
    type: object
    x-go-name: XLFileAPI
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlfileDB:
    description: |-
      It incorporates the GORM ID, basic fields from the model (because they can be serialized),
      the encoded version of pointers
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name_Data:
        $ref: '#/definitions/NullString'
      NbSheets_Data:
        $ref: '#/definitions/NullInt64'
      UpdatedAt:
        format: date-time
        type: string
    title: XLFileDB describes a xlfile in the database
    type: object
    x-go-name: XLFileDB
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlrowAPI:
    description: |-
      for POST, API, one needs the fields of the model as well as the fields
      from associations ("Has One" and "Has Many") that are generated to
      fullfill the ORM requirements for associations
    properties:
      Cells:
        items:
          $ref: '#/definitions/XLCell'
        type: array
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name:
        type: string
      NbCols:
        format: int64
        type: integer
      RowIndex:
        format: int64
        type: integer
      UpdatedAt:
        format: date-time
        type: string
      XLSheet_RowsDBID:
        $ref: '#/definitions/NullInt64'
      XLSheet_RowsDBID_Index:
        $ref: '#/definitions/NullInt64'
    title: XLRowAPI is the input in POST API
    type: object
    x-go-name: XLRowAPI
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlrowDB:
    description: |-
      It incorporates the GORM ID, basic fields from the model (because they can be serialized),
      the encoded version of pointers
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      Name_Data:
        $ref: '#/definitions/NullString'
      NbCols_Data:
        $ref: '#/definitions/NullInt64'
      RowIndex_Data:
        $ref: '#/definitions/NullInt64'
      UpdatedAt:
        format: date-time
        type: string
      XLSheet_RowsDBID:
        $ref: '#/definitions/NullInt64'
      XLSheet_RowsDBID_Index:
        $ref: '#/definitions/NullInt64'
    title: XLRowDB describes a xlrow in the database
    type: object
    x-go-name: XLRowDB
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlsheetAPI:
    description: |-
      for POST, API, one needs the fields of the model as well as the fields
      from associations ("Has One" and "Has Many") that are generated to
      fullfill the ORM requirements for associations
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      MaxCol:
        format: int64
        type: integer
      MaxRow:
        format: int64
        type: integer
      Name:
        type: string
      NbRows:
        format: int64
        type: integer
      Rows:
        items:
          $ref: '#/definitions/XLRow'
        type: array
      SheetCells:
        items:
          $ref: '#/definitions/XLCell'
        type: array
      UpdatedAt:
        format: date-time
        type: string
      XLFile_SheetsDBID:
        $ref: '#/definitions/NullInt64'
      XLFile_SheetsDBID_Index:
        $ref: '#/definitions/NullInt64'
    title: XLSheetAPI is the input in POST API
    type: object
    x-go-name: XLSheetAPI
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
  xlsheetDB:
    description: |-
      It incorporates the GORM ID, basic fields from the model (because they can be serialized),
      the encoded version of pointers
    properties:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt:
        $ref: '#/definitions/DeletedAt'
      ID:
        format: uint64
        type: integer
      MaxCol_Data:
        $ref: '#/definitions/NullInt64'
      MaxRow_Data:
        $ref: '#/definitions/NullInt64'
      Name_Data:
        $ref: '#/definitions/NullString'
      NbRows_Data:
        $ref: '#/definitions/NullInt64'
      UpdatedAt:
        format: date-time
        type: string
      XLFile_SheetsDBID:
        $ref: '#/definitions/NullInt64'
      XLFile_SheetsDBID_Index:
        $ref: '#/definitions/NullInt64'
    title: XLSheetDB describes a xlsheet in the database
    type: object
    x-go-name: XLSheetDB
    x-go-package: github.com/fullstack-lang/gongxlsx/go/orm
host: localhost:8080
info:
  description: gongxlsx exposes types of a package.
  title: gongxlsx backend API
  version: "0.1"
paths:
  /commitnb:
    get:
      operationId: GetLastCommitNb
      tags:
      - backrepo
  /pushfromfrontnb:
    get:
      operationId: GetLastPushFromFrontNb
      tags:
      - backrepo
  /xlcells:
    get:
      description: Get all xlcells
      operationId: getXLCells
      responses:
        "200":
          $ref: '#/responses/xlcellDBsResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlcells
    post:
      consumes:
      - application/json
      description: Creates a xlcell
      operationId: postXLCell
      parameters:
      - description: The XLCell to submit or modify
        in: body
        name: XLCell
        schema:
          $ref: '#/definitions/xlcellAPI'
      produces:
      - application/json
      responses:
        "200":
          $ref: '#/responses/xlcellDBResponse'
      tags:
      - xlcells
  /xlcells/{ID}:
    delete:
      description: Delete a xlcell
      operationId: deleteXLCell
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlcells
    get:
      operationId: getXLCell
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        "200":
          $ref: '#/responses/xlcellDBResponse'
        default:
          $ref: '#/responses/genericError'
      summary: Gets the details for a xlcell.
      tags:
      - xlcells
    patch:
      description: Update a xlcell
      operationId: updateXLCell
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      - description: The XLCell to submit or modify
        in: body
        name: XLCell
        schema:
          $ref: '#/definitions/xlcellAPI'
      responses:
        "200":
          $ref: '#/responses/xlcellDBResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlcells
  /xlfiles:
    get:
      description: Get all xlfiles
      operationId: getXLFiles
      responses:
        "200":
          $ref: '#/responses/xlfileDBsResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlfiles
    post:
      consumes:
      - application/json
      description: Creates a xlfile
      operationId: postXLFile
      parameters:
      - description: The XLFile to submit or modify
        in: body
        name: XLFile
        schema:
          $ref: '#/definitions/xlfileAPI'
      produces:
      - application/json
      responses:
        "200":
          $ref: '#/responses/xlfileDBResponse'
      tags:
      - xlfiles
  /xlfiles/{ID}:
    delete:
      description: Delete a xlfile
      operationId: deleteXLFile
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlfiles
    get:
      operationId: getXLFile
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        "200":
          $ref: '#/responses/xlfileDBResponse'
        default:
          $ref: '#/responses/genericError'
      summary: Gets the details for a xlfile.
      tags:
      - xlfiles
    patch:
      description: Update a xlfile
      operationId: updateXLFile
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      - description: The XLFile to submit or modify
        in: body
        name: XLFile
        schema:
          $ref: '#/definitions/xlfileAPI'
      responses:
        "200":
          $ref: '#/responses/xlfileDBResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlfiles
  /xlrows:
    get:
      description: Get all xlrows
      operationId: getXLRows
      responses:
        "200":
          $ref: '#/responses/xlrowDBsResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlrows
    post:
      consumes:
      - application/json
      description: Creates a xlrow
      operationId: postXLRow
      parameters:
      - description: The XLRow to submit or modify
        in: body
        name: XLRow
        schema:
          $ref: '#/definitions/xlrowAPI'
      produces:
      - application/json
      responses:
        "200":
          $ref: '#/responses/xlrowDBResponse'
      tags:
      - xlrows
  /xlrows/{ID}:
    delete:
      description: Delete a xlrow
      operationId: deleteXLRow
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlrows
    get:
      operationId: getXLRow
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        "200":
          $ref: '#/responses/xlrowDBResponse'
        default:
          $ref: '#/responses/genericError'
      summary: Gets the details for a xlrow.
      tags:
      - xlrows
    patch:
      description: Update a xlrow
      operationId: updateXLRow
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      - description: The XLRow to submit or modify
        in: body
        name: XLRow
        schema:
          $ref: '#/definitions/xlrowAPI'
      responses:
        "200":
          $ref: '#/responses/xlrowDBResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlrows
  /xlsheets:
    get:
      description: Get all xlsheets
      operationId: getXLSheets
      responses:
        "200":
          $ref: '#/responses/xlsheetDBsResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlsheets
    post:
      consumes:
      - application/json
      description: Creates a xlsheet
      operationId: postXLSheet
      parameters:
      - description: The XLSheet to submit or modify
        in: body
        name: XLSheet
        schema:
          $ref: '#/definitions/xlsheetAPI'
      produces:
      - application/json
      responses:
        "200":
          $ref: '#/responses/xlsheetDBResponse'
      tags:
      - xlsheets
  /xlsheets/{ID}:
    delete:
      description: Delete a xlsheet
      operationId: deleteXLSheet
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlsheets
    get:
      operationId: getXLSheet
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      responses:
        "200":
          $ref: '#/responses/xlsheetDBResponse'
        default:
          $ref: '#/responses/genericError'
      summary: Gets the details for a xlsheet.
      tags:
      - xlsheets
    patch:
      description: Update a xlsheet
      operationId: updateXLSheet
      parameters:
      - description: The ID of the order
        format: int64
        in: path
        name: ID
        required: true
        type: integer
      - description: The XLSheet to submit or modify
        in: body
        name: XLSheet
        schema:
          $ref: '#/definitions/xlsheetAPI'
      responses:
        "200":
          $ref: '#/responses/xlsheetDBResponse'
        default:
          $ref: '#/responses/genericError'
      tags:
      - xlsheets
produces:
- application/json
responses:
  genericError:
    description: |-
      A GenericError is the default error message that is generated.
      For certain status codes there are more appropriate error structures.
    schema:
      properties:
        code:
          format: int32
          type: integer
          x-go-name: Code
        message:
          type: string
          x-go-name: Message
      type: object
  validationError:
    description: |-
      A ValidationError is an that is generated for validation failures.
      It has the same fields as a generic error but adds a Field property.
    schema:
      properties:
        code:
          format: int32
          type: integer
          x-go-name: Code
        field:
          type: string
          x-go-name: Field
        message:
          type: string
          x-go-name: Message
      type: object
  xlcellDBResponse:
    description: XLCellDBResponse provides response
    headers:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt: {}
      ID:
        format: uint64
        type: integer
      Name_Data:
        description: |-
          insertion for basic fields declaration
          Declation for basic field xlcellDB.Name {{BasicKind}} (to be completed)
      UpdatedAt:
        format: date-time
        type: string
      X_Data:
        description: Declation for basic field xlcellDB.X {{BasicKind}} (to be completed)
      XLRow_CellsDBID:
        description: |-
          insertion for pointer fields encoding declaration
          Implementation of a reverse ID for field XLRow{}.Cells []*XLCell
      XLRow_CellsDBID_Index:
        description: implementation of the index of the withing the slice
      XLSheet_SheetCellsDBID:
        description: Implementation of a reverse ID for field XLSheet{}.SheetCells []*XLCell
      XLSheet_SheetCellsDBID_Index:
        description: implementation of the index of the withing the slice
      Y_Data:
        description: Declation for basic field xlcellDB.Y {{BasicKind}} (to be completed)
    schema:
      $ref: '#/definitions/NullInt64'
  xlcellDBsResponse:
    description: XLCellDBs arrays xlcellDBs
    schema:
      items:
        $ref: '#/definitions/xlcellDB'
      type: array
  xlfileDBResponse:
    description: XLFileDBResponse provides response
    headers:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt: {}
      ID:
        format: uint64
        type: integer
      Name_Data:
        description: |-
          insertion for basic fields declaration
          Declation for basic field xlfileDB.Name {{BasicKind}} (to be completed)
      NbSheets_Data:
        description: Declation for basic field xlfileDB.NbSheets {{BasicKind}} (to be completed)
      UpdatedAt:
        format: date-time
        type: string
    schema:
      $ref: '#/definitions/NullInt64'
  xlfileDBsResponse:
    description: XLFileDBs arrays xlfileDBs
    schema:
      items:
        $ref: '#/definitions/xlfileDB'
      type: array
  xlrowDBResponse:
    description: XLRowDBResponse provides response
    headers:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt: {}
      ID:
        format: uint64
        type: integer
      Name_Data:
        description: |-
          insertion for basic fields declaration
          Declation for basic field xlrowDB.Name {{BasicKind}} (to be completed)
      NbCols_Data:
        description: Declation for basic field xlrowDB.NbCols {{BasicKind}} (to be completed)
      RowIndex_Data:
        description: Declation for basic field xlrowDB.RowIndex {{BasicKind}} (to be completed)
      UpdatedAt:
        format: date-time
        type: string
      XLSheet_RowsDBID:
        description: |-
          insertion for pointer fields encoding declaration
          Implementation of a reverse ID for field XLSheet{}.Rows []*XLRow
      XLSheet_RowsDBID_Index:
        description: implementation of the index of the withing the slice
    schema:
      $ref: '#/definitions/NullInt64'
  xlrowDBsResponse:
    description: XLRowDBs arrays xlrowDBs
    schema:
      items:
        $ref: '#/definitions/xlrowDB'
      type: array
  xlsheetDBResponse:
    description: XLSheetDBResponse provides response
    headers:
      CreatedAt:
        format: date-time
        type: string
      DeletedAt: {}
      ID:
        format: uint64
        type: integer
      MaxCol_Data:
        description: Declation for basic field xlsheetDB.MaxCol {{BasicKind}} (to be completed)
      MaxRow_Data:
        description: Declation for basic field xlsheetDB.MaxRow {{BasicKind}} (to be completed)
      Name_Data:
        description: |-
          insertion for basic fields declaration
          Declation for basic field xlsheetDB.Name {{BasicKind}} (to be completed)
      NbRows_Data:
        description: Declation for basic field xlsheetDB.NbRows {{BasicKind}} (to be completed)
      UpdatedAt:
        format: date-time
        type: string
      XLFile_SheetsDBID:
        description: |-
          insertion for pointer fields encoding declaration
          Implementation of a reverse ID for field XLFile{}.Sheets []*XLSheet
      XLFile_SheetsDBID_Index:
        description: implementation of the index of the withing the slice
    schema:
      $ref: '#/definitions/NullInt64'
  xlsheetDBsResponse:
    description: XLSheetDBs arrays xlsheetDBs
    schema:
      items:
        $ref: '#/definitions/xlsheetDB'
      type: array
schemes:
- http
securityDefinitions:
  basic:
    type: basic
swagger: "2.0"
